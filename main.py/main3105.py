#a

#b
agest = ages

#c
ls_a= [1,2,3]
# print("max of the list: ", max(ls_a))
max_age = num

#d
len_name = len(name[0]) #set flag: maximum lengthof name
for name in names:
    check = len(name)
    if check>longest_name:
        len_name=check
        longest_name = name
for i in range(len(name )):
    print(i)
    check = len(nam[i]) #check length of each 
    check = len(name)
    if check>longest_name:
        len_name=check
        longest_name = name
    
        
print("The longest naem is: ", longest_name)

def check_a():
    pass
